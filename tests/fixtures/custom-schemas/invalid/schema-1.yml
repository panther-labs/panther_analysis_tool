# NOTE: this is a valid schema in order to test the behavior
# when both valid and invalid schemas are present.
schema: Custom.SampleSchema1
description: "Sample Schema 1"
referenceURL: "https://runpanther.io"
version: 0
fields:
  - name: time
    description: Event timestamp
    required: true
    type: timestamp
    timeFormat: rfc3339
    isEventTime: true
  - name: method
    description: The HTTP method used for the request
    type: string
